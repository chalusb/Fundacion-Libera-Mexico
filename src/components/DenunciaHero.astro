---
export interface Props {
  backgroundColor?: string;
  titleColor?: string;
  subtitleColor?: string;
  backgroundImage?: string;
}

const {
  backgroundColor = "bg-gray-900",
  titleColor = "text-white",
  subtitleColor = "text-gray-200",
  backgroundImage = "/hero-3.jpg",
} = Astro.props;
---

<section
  class={`${backgroundColor} relative h-[80vh] mt-20 flex items-center justify-center overflow-hidden`}
>
  <!-- Background image -->
  <div
    class="absolute inset-0 bg-cover bg-no-repeat"
    style={`background-image: url('${backgroundImage}'); background-position: center 40%;`}
  >
    <div class="absolute inset-0 bg-black/50"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <div class="max-w-4xl mx-auto">
      <!-- Emergency alert -->
      <div
        class="bg-red-600/20 border border-red-600/30 rounded-lg p-6 mb-8 max-w-2xl mx-auto"
      >
        <div class="flex items-center justify-center mb-4">
          <svg
            class="w-8 h-8 text-red-400 mr-3"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z"
            ></path>
          </svg>
          <h2 class="text-red-400 text-2xl font-bold">🚨 EMERGENCIA</h2>
        </div>
        <p class="text-red-300 text-lg font-semibold">
          Si eres víctima de trata o conoces a alguien que lo sea, llama al <strong
            >911</strong
          > o al <strong>01 800 5533 000</strong>
        </p>
      </div>

      <!-- Main title -->
      <h1
        class={`${titleColor} text-4xl sm:text-5xl lg:text-6xl font-bold mb-8 leading-tight`}
      >
        Denuncia
      </h1>

      <!-- Subtitle -->
      <p
        class={`${subtitleColor} text-lg sm:text-xl leading-relaxed max-w-3xl mx-auto`}
      >
        Tu voz es importante. Denuncia de forma segura, confidencial y gratuita.
        Juntos podemos hacer la diferencia.
      </p>
    </div>
  </div>
</section>
