---
export interface Props {
  backgroundColor?: string;
  titleColor?: string;
  textColor?: string;
  cardBackgroundColor?: string;
}

const {
  backgroundColor = "bg-white",
  titleColor = "text-gray-900",
  textColor = "text-gray-700",
  cardBackgroundColor = "bg-white",
} = Astro.props;
---

<section class={`${backgroundColor} py-20 px-4 sm:px-6 lg:px-8 mt-20`}>
  <div class="max-w-7xl mx-auto">
    <!-- Section title -->
    <div class="text-center mb-16">
      <h1 class={`${titleColor} text-3xl sm:text-4xl font-bold mb-4`}>
        Contáctanos
      </h1>
      <p class="text-lg text-gray-600 max-w-3xl mx-auto">
        Estamos aquí para escucharte. Si tienes dudas, quieres colaborar o
        necesitas ayuda, no dudes en contactarnos.
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
      <!-- Contact Form -->
      <div
        class={`${cardBackgroundColor} rounded-2xl p-8 shadow-lg border border-gray-100`}
      >
        <h2 class={`${titleColor} text-2xl font-bold mb-6`}>
          Envíanos un mensaje
        </h2>

        <form class="space-y-6" id="contact-form">
          <!-- Name -->
          <div>
            <label
              for="name"
              class="block text-sm font-medium text-gray-700 mb-2"
            >
              Nombre completo *
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-foundation-primary focus:border-transparent transition-colors duration-200"
              placeholder="Tu nombre completo"
            />
          </div>

          <!-- Email -->
          <div>
            <label
              for="email"
              class="block text-sm font-medium text-gray-700 mb-2"
            >
              Correo electrónico *
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-foundation-primary focus:border-transparent transition-colors duration-200"
              placeholder="tu@email.com"
            />
          </div>

          <!-- Phone -->
          <div>
            <label
              for="phone"
              class="block text-sm font-medium text-gray-700 mb-2"
            >
              Teléfono
            </label>
            <input
              type="tel"
              id="phone"
              name="phone"
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-foundation-primary focus:border-transparent transition-colors duration-200"
              placeholder="Tu número de teléfono"
            />
          </div>

          <!-- Subject -->
          <div>
            <label
              for="subject"
              class="block text-sm font-medium text-gray-700 mb-2"
            >
              Asunto *
            </label>
            <select
              id="subject"
              name="subject"
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-foundation-primary focus:border-transparent transition-colors duration-200"
            >
              <option value="">Selecciona un asunto</option>
              <option value="denuncia">Denuncia</option>
              <option value="voluntariado">Voluntariado</option>
              <option value="donacion">Donación</option>
              <option value="informacion">Información general</option>
              <option value="colaboracion">Colaboración</option>
              <option value="otro">Otro</option>
            </select>
          </div>

          <!-- Message -->
          <div>
            <label
              for="message"
              class="block text-sm font-medium text-gray-700 mb-2"
            >
              Mensaje *
            </label>
            <textarea
              id="message"
              name="message"
              rows="5"
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-foundation-primary focus:border-transparent transition-colors duration-200 resize-vertical"
              placeholder="Cuéntanos cómo podemos ayudarte..."></textarea>
          </div>

          <!-- Submit Button -->
          <button
            type="submit"
            class="w-full bg-foundation-primary hover:bg-foundation-primary/90 text-white font-semibold py-4 px-6 rounded-lg transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-foundation-primary focus:ring-offset-2"
          >
            Enviar mensaje
          </button>
        </form>
      </div>

      <!-- Contact Information -->
      <div class="space-y-8">
        <!-- Contact details -->
        <div
          class={`${cardBackgroundColor} rounded-2xl p-8 shadow-lg border border-gray-100`}
        >
          <h2 class={`${titleColor} text-2xl font-bold mb-6`}>
            Información de contacto
          </h2>

          <div class="space-y-6">
            <!-- Email -->
            <div class="flex items-start space-x-4">
              <div class="bg-foundation-primary/10 p-3 rounded-lg">
                <svg
                  class="w-6 h-6 text-foundation-primary"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                  ></path>
                </svg>
              </div>
              <div>
                <h3 class={`${titleColor} font-semibold mb-1`}>
                  Correo electrónico
                </h3>
                <a
                  href="mailto:contacto@liberamexico.org"
                  class={`${textColor} hover:text-foundation-primary transition-colors duration-200`}
                >
                  contacto@liberamexico.org
                </a>
              </div>
            </div>

            <!-- Phone -->
            <div class="flex items-start space-x-4">
              <div class="bg-foundation-primary/10 p-3 rounded-lg">
                <svg
                  class="w-6 h-6 text-foundation-primary"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
                  ></path>
                </svg>
              </div>
              <div>
                <h3 class={`${titleColor} font-semibold mb-1`}>
                  Teléfono/WhatsApp
                </h3>
                <a
                  href="tel:+525589392792"
                  class={`${textColor} hover:text-foundation-primary transition-colors duration-200`}
                >
                  55 8939 2792
                </a>
              </div>
            </div>

            <!-- Address -->
            <div class="flex items-start space-x-4">
              <div class="bg-foundation-primary/10 p-3 rounded-lg">
                <svg
                  class="w-6 h-6 text-foundation-primary"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
                  ></path>
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                </svg>
              </div>
              <div>
                <h3 class={`${titleColor} font-semibold mb-1`}>Dirección</h3>
                <p class={`${textColor}`}>
                  Av Santa Fe 505, Cuajimalpa México, México, CP.
                </p>
              </div>
            </div>
          </div>
        </div>

        <!-- Emergency contact -->
        <div class="bg-red-50 border border-red-200 rounded-2xl p-6">
          <div class="flex items-center mb-4">
            <svg
              class="w-6 h-6 text-red-600 mr-3"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z"
              ></path>
            </svg>
            <h3 class="text-red-800 font-bold text-lg">🚨 Emergencia</h3>
          </div>
          <p class="text-red-700 text-sm mb-3">
            Si eres víctima de trata o conoces a alguien que lo sea:
          </p>
          <div class="space-y-2">
            <div class="flex items-center">
              <span class="text-red-600 font-bold mr-2">911</span>
              <span class="text-red-700 text-sm">Emergencias</span>
            </div>
            <div class="flex items-center">
              <span class="text-red-600 font-bold mr-2">01 800 5533 000</span>
              <span class="text-red-700 text-sm">Línea nacional</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Manejar el formulario de contacto
  document.addEventListener("DOMContentLoaded", () => {
    const contactForm = document.getElementById(
      "contact-form"
    ) as HTMLFormElement;

    if (contactForm) {
      contactForm.addEventListener("submit", (e) => {
        e.preventDefault();

        const formData = new FormData(contactForm);
        const submitButton = contactForm.querySelector(
          'button[type="submit"]'
        ) as HTMLButtonElement;

        if (!submitButton) return;

        // Cambiar el texto del botón para mostrar que se está procesando
        const originalText = submitButton.textContent;
        submitButton.textContent = "Enviando...";
        submitButton.disabled = true;

        // Simular envío (aquí conectarías con tu servicio de email)
        setTimeout(() => {
          alert("¡Gracias por tu mensaje! Te contactaremos pronto.");
          contactForm.reset();
          if (submitButton && originalText) {
            submitButton.textContent = originalText;
            submitButton.disabled = false;
          }
        }, 1500);
      });
    }
  });
</script>
