---
export interface Props {
  backgroundColor?: string;
  titleColor?: string;
  textColor?: string;
  cardBackgroundColor?: string;
}

const {
  backgroundColor = "bg-white",
  titleColor = "text-gray-900",
  textColor = "text-gray-700",
  cardBackgroundColor = "bg-gray-50",
} = Astro.props;

const programs = [
  
  {
    category: "Salud Mental",
    description: "Acompañamiento psicológico y psiquiátrico especializado",
    icon: "🧠",
    color: "bg-green-500"
  },
  {
    category: "Educativo",
    description: "Programas de educación y formación para la reinserción social",
    icon: "📚",
    color: "bg-purple-500"
  },
  {
    category: "Jurídico",
    description: "Asesoría legal y acompañamiento en procesos judiciales",
    icon: "⚖️",
    color: "bg-red-500"
  },
  {
    category: "Médico",
    description: "Atención médica integral y especializada",
    icon: "🏥",
    color: "bg-teal-500"
  },
  {
    category: "Recreativo",
    description: "Actividades recreativas y de desarrollo personal",
    icon: "🎨",
    color: "bg-orange-500"
  },
  {
    category: "Prevención",
    description: "Pláticas y capacitaciones para prevenir la trata de personas en escuelas y/o empresas",
    icon: "🛡️",
    color: "bg-blue-500"
  },
];
---

<section class={`${backgroundColor} py-20 px-4 sm:px-6 lg:px-8`}>
  <div class="max-w-7xl mx-auto">
    <!-- Section title -->
    <div class="text-center mb-16">
      <h2 class={`${titleColor} text-3xl sm:text-4xl font-bold mb-4`}>
        Nuestros Programas
      </h2>
      <p class="text-lg text-gray-600 max-w-3xl mx-auto">
        Un enfoque integral que abarca desde la prevención hasta la rehabilitación de las niñas, niños y adolescentes que atendemos.
      </p>
    </div>

    <!-- Programs grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {programs.map((program) => (
        <div class={`${cardBackgroundColor} rounded-2xl p-8 shadow-lg hover:shadow-xl transition-all duration-300 border border-gray-100 group`}>
          <!-- Icon -->
          <div class="text-center mb-6">
            <div class={`${program.color} w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300`}>
              <span class="text-3xl">{program.icon}</span>
            </div>
          </div>
          
          <!-- Content -->
          <div class="text-center">
            <h3 class={`${titleColor} text-xl font-bold mb-3`}>
              {program.category}
            </h3>
            <p class={`${textColor} leading-relaxed`}>
              {program.description}
            </p>
          </div>
        </div>
      ))}
    </div>

    <!-- Additional info -->
    <div class="mt-16">
      <div class="bg-gradient-to-r from-foundation-primary/10 to-foundation-primary/5 rounded-2xl p-8 lg:p-12 text-center">
        <h3 class={`${titleColor} text-2xl font-bold mb-4`}>
          Enfoque Integral
        </h3>
        <p class={`${textColor} text-lg leading-relaxed max-w-4xl mx-auto`}>
          Nuestros programas trabajan de manera coordinada para brindar un acompañamiento completo 
          que va desde la prevención y detección temprana, hasta la rehabilitación integral y la 
          reinserción social exitosa de cada persona que atendemos.
        </p>
      </div>
    </div>

    <!-- Call to action -->
    <div class="mt-12 text-center">
      <p class={`${textColor} text-lg mb-6`}>
        ¿Necesitas ayuda o quieres conocer más sobre nuestros programas?
      </p>
      <a 
        href="mailto:contacto@liberamexico.org" 
        class="inline-flex items-center bg-foundation-primary hover:bg-foundation-primary/90 text-white font-semibold py-4 px-8 rounded-lg transition-colors duration-200 shadow-lg hover:shadow-xl"
      >
        Contáctanos
      </a>
    </div>
  </div>
</section>
